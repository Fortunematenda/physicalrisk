'use client';
import { SimpleCrud } from '@/components/simple-crud';
export default function FileTypesPage(){return <SimpleCrud title="File Types" description="Control permitted extensions, MIME types, size limits and whether metadata extraction is allowed." endpoint="/file-types" fields={[{key:'extension',label:'Extension',required:true,placeholder:'docx'},{key:'label',label:'Label',required:true},{key:'mimeTypes',label:'MIME types JSON',type:'json',required:true,defaultValue:'[]'},{key:'maxSizeMb',label:'Maximum size (MB)',type:'number',defaultValue:50},{key:'allowMetadataExtraction',label:'Allow metadata extraction',type:'checkbox',defaultValue:true},{key:'active',label:'Active',type:'checkbox',defaultValue:true}]} columns={[{key:'extension',label:'Extension',render:(item)=><span className="mono">.{item.extension}</span>},{key:'label',label:'Label'},{key:'maxSizeMb',label:'Max MB'},{key:'allowMetadataExtraction',label:'Extraction',render:(item)=><span className="badge">{item.allowMetadataExtraction?'YES':'NO'}</span>},{key:'active',label:'Status'}]}/>}
